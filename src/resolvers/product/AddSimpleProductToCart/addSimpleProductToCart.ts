import { AddSimpleProductToCartMutationVariables } from "src/schema";

const AddSimpleProductToCart = (props: {
  AddSimpleProductToCartMutationVariables;
}) => {
  // Look docs for more info about how to fill this function

  return { data: {}, loading: false, error: undefined };
};

export default AddSimpleProductToCart;
